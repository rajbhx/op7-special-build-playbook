# DD4 — android 10 install can fail on testOnly or signature mismatch

**Section**: B. Android 10 & runtime stability
**Keywords**: aapt, android, android10, badging, consistent, debug, debug_signing_key, fail, flag, gate, injected, install, keystore, mismatch, per-build, rejects, runtime, secret, signatures, signing, stability, stable, testonly

## Problem

android 10 install can fail on testOnly or signature mismatch

## Root cause

injected testOnly flag / per-build debug keystore

## Solution

aapt badging gate rejects testOnly; stable DEBUG_SIGNING_KEY secret for consistent signatures

<!-- generated by build_notes.py; do not hand-edit -->
