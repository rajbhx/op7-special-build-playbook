# DD5 — repeated denoise jobs grow memory on OP7 (one InferenceSession leaked per job)

**Section**: B. Android 10 & runtime stability
**Keywords**: android, calls, creates, declare, deepfilternet, denoiser, finally, flow, grow, handledenoise, inferencesession, jobs, leaked, memory, memory-leak, null-safe, onnx, outside, patch, patch-001, process.tsx, recording, releases, repeated, runtime, session, stability

## Problem

repeated denoise jobs grow memory on OP7 (one InferenceSession leaked per job)

## Root cause

process.tsx handleDenoise creates a DeepFilterNet and never calls release(); recording flow already releases via ref

## Solution

OP7 patch 001 - declare denoiser outside try, release() in finally (session is null-safe)

<!-- generated by build_notes.py; do not hand-edit -->
