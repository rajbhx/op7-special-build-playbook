# DD6 — deepdenoiser CI build fails compiling @siteed/audio-studio (Promise.reject override mismatch)

**Section**: A. Build pipeline & dependencies
**Keywords**: audio-studio, build, bun.lock, compile, compile-error, compiling, deepdenoiser, dependencies, expo-modules-core, fails, fresh, lockfile, longer, matches, mismatch, onnx, overrides, passes, patch-package, patches, pipeline, promise.reject, reject, resolved, siteed, string, target, upstream, verify

## Problem

deepdenoiser CI build fails compiling @siteed/audio-studio (Promise.reject override mismatch)

## Root cause

fresh bun.lock resolved siteed 3.2.1; its reject(String?,...) no longer matches expo-modules-core Promise.reject(String,...); upstream patch-package patches target 3.0.3

## Solution

pin @siteed/audio-studio to 3.0.3 in overrides/bun.lock; verify compile passes

<!-- generated by build_notes.py; do not hand-edit -->
