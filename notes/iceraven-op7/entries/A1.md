# A1 — destination path 'upstream' already exists

**Section**: A. Build pipeline
**Keywords**: build, cloned, destination, dirs, exists, instead, mirror, paths, pipeline, repo, tracked, upstream, workflow

## Problem

destination path 'upstream' already exists

## Root cause

workflow cloned into 'upstream/', a tracked dir of this repo

## Solution

clone into 'mirror/' instead; keep clone dirs out of tracked paths

<!-- generated by build_notes.py; do not hand-edit -->
