# A11 — full build ~40 min on free runner

**Section**: A. Build pipeline
**Keywords**: abi, abis, arm64-only, build, build-time, caches, caching, download, fast, free, free-tier, geckoview, gradle, maven, parallel, pipeline, runner, rust, split, stable, true, validation

## Problem

full build ~40 min on free runner

## Root cause

GeckoView AAR download + R8 + all ABIs

## Solution

arm64-only split, Gradle/Rust/Maven caches, fast=true (no R8) for validation, --parallel when stable

<!-- generated by build_notes.py; do not hand-edit -->
