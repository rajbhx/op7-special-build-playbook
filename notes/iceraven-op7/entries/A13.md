# A13 — Expo SDK 55 lockfile drift broke compile (@siteed/audio-studio 3.2.1)

**Section**: A. Build pipeline
**Keywords**: audio-studio, broke, build, bun, bun.lock, compile, compile-error, drift, expo, fresh, lockfile, overrides, pipeline, promise.reject, resolved, siteed, string, verify, whose

## Problem

Expo SDK 55 lockfile drift broke compile (@siteed/audio-studio 3.2.1)

## Root cause

fresh bun.lock resolved siteed 3.2.1 whose Promise.reject(String?,...)

## Solution

pin @siteed/audio-studio=3.0.3 via overrides/bun.lock; verify compile

<!-- generated by build_notes.py; do not hand-edit -->
