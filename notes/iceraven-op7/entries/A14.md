# A14 — ONNX InferenceSession leaked per denoise job (processing screen)

**Section**: A. Build pipeline
**Keywords**: build, calls, claim, creates, deepfilternet, denoise, finally, inferencesession, leaked, memory-leak, onnx, patch, perf, pipeline, process.tsx, processing, recording, release, screen, stability

## Problem

ONNX InferenceSession leaked per denoise job (processing screen)

## Root cause

process.tsx creates DeepFilterNet and never calls release(); recording

## Solution

OP7 patch 001 — release() in finally (stability fix, not perf claim)

<!-- generated by build_notes.py; do not hand-edit -->
