# A15 — rainManager APK has no native-code badging entry (lspatch libs in assets)

**Section**: A. Build pipeline
**Keywords**: abi, assets, badging, build, checks, discord, entry, gate, injected, libs, lspatch, manager, native-code, package, patched, pipeline, rain, rainmanager, testonly, validation

## Problem

rainManager APK has no native-code badging entry (lspatch libs in assets)

## Root cause

lspatch .so injected into patched Discord APK at patch time, not used by

## Solution

validation gate for manager checks package/SDK/testOnly only

<!-- generated by build_notes.py; do not hand-edit -->
