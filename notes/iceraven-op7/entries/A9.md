# A9 — 'native-code' badging line missing

**Section**: A. Build pipeline
**Keywords**: badging, build, configure, explicitly, fail, gates, ignored, line, loudly, missing, native-code, pipeline, selection, splits.abi, validation, versions

## Problem

'native-code' badging line missing

## Root cause

ABI selection ignored by some AGP versions

## Solution

validation gates fail loudly; configure splits.abi explicitly

<!-- generated by build_notes.py; do not hand-edit -->
