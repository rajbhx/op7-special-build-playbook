# C1 — App not installed

**Section**: C. Device and install
**Keywords**: abi, android, app-not-installed, arm64-only, badging, blocks, build, device, flagged, gate, injected, install, installed, installs, rejects, remove, splits.abi, studio-injected, testonly, true, verify

## Problem

App not installed

## Root cause

APK flagged android:testOnly=true (Studio-injected build flag); Android 10 blocks UI installs

## Solution

remove injected flag; splits.abi arm64-only; CI badging gate rejects testOnly; verify pm install -r without -t

<!-- generated by build_notes.py; do not hand-edit -->
