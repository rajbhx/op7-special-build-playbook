# D10 — proot files invisible to the real device shell (shizuku)

**Section**: D. Measurement and transfer
**Keywords**: app-private, bind, copy, curl, data, device, download, emulated, gptos.intelligence.assistant, http, http.server, inside, install, invisible, local, loopback, measurement, network, nohup, persistent, proot, pull, python3, real, runs, sdcard, server, setsid, shares, shell, shizuku, size, storage, transfer, verify

## Problem

proot files invisible to the real device shell (shizuku)

## Root cause

proot runs inside the app (gptos.intelligence.assistant); its /storage/emulated/0 is an app-private bind, not the real /sdcard

## Solution

proot shares the device network, so serve the APK with a persistent loopback HTTP server (setsid nohup python3 -m http.server) and pull with curl on the real shell to /sdcard/Download/op7/; verify size; copy to /data/local/tmp then pm install -r

<!-- generated by build_notes.py; do not hand-edit -->
