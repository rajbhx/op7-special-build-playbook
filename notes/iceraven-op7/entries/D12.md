# D12 — summary CPU parse showed a fake 1034% spike in the probe

**Section**: D. Measurement and transfer
**Keywords**: against, column, cpu, cpu-all.txt, digits, fake, grabbed, grep, leading, measurement, naive, number, output, parse, parsing, peak, per-process, probe, report, samples, showed, spike, summary, transfer, trusting, verify, wrong

## Problem

summary CPU parse showed a fake 1034% spike in the probe

## Root cause

naive grep of leading digits grabbed the wrong column from top output

## Solution

report peak from raw per-process samples; verify against cpu-all.txt before trusting a number

<!-- generated by build_notes.py; do not hand-edit -->
