# D13 — "3/6/10 tabs" memory captures were invalid — no new tabs were actually opened

**Section**: D. Measurement and transfer
**Keywords**: a11y, actually, already-foreground, appeared, awesome, captures, confirmed, correct, counts, creating, creation, drive, dump, existing, fenix, iceraven, instead, intent, invalid, manually, measurement, measuring, memory, node, observation, opened, opens, path, real, repeated, reuses, start, tabs, task, toolbar, transfer, ui-automation, uiautomator, user, validate, view, view-intent, were

## Problem

"3/6/10 tabs" memory captures were invalid — no new tabs were actually opened

## Root cause

repeated `am start -a VIEW -d <url>` on an already-foreground Fenix reuses the existing tab/task instead of creating new tabs; user confirmed no new tabs appeared

## Solution

validate tab creation before measuring (uiautomator dump + user observation); VIEW-intent counts are not tab counts. Correct path: user opens tabs manually, or tap the real "New tab" toolbar node (from the Iceraven a11y dump) then drive the awesome bar

<!-- generated by build_notes.py; do not hand-edit -->
