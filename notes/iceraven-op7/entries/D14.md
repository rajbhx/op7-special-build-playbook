# D14 — a11y dumps can show the WRONG app — the gptos assistant overlay, not the browser

**Section**: D. Measurement and transfer
**Keywords**: a11y, assistant, browser, captured, confirm, coordinates, dumps, floating, gptos, grep, host, iceraven, mcurrentfocus, measurement, nodes, overlay, package, renders, shizuku, show, taps, transfer, trusting, ui-automation, uiautomator, wrong

## Problem

a11y dumps can show the WRONG app — the gptos assistant overlay, not the browser

## Root cause

the assistant host app renders its own floating UI on top; `uiautomator dump` captured that package's nodes

## Solution

grep the dump for package="...iceraven..." and confirm mCurrentFocus before trusting coordinates/taps

<!-- generated by build_notes.py; do not hand-edit -->
