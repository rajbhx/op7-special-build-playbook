# D15 — needed real per-tab memory data but VIEW intents cannot create tabs in a foreground Fenix

**Section**: D. Measurement and transfer
**Keywords**: as-is, automation, baseline_capture.sh, blocked, cannot, captures, collaboration, collaborative, create, current, data, fenix, force-stop, foreground, hand, host, intents, measurement, meminfo, memory, needed, opens, overlay, per-tab, processes, real, repeated, reuse, state, tabs, transfer, user, view

## Problem

needed real per-tab memory data but VIEW intents cannot create tabs in a foreground Fenix

## Root cause

repeated VIEW intents reuse the current tab (see D13); UI automation is blocked by the host app overlay (D14)

## Solution

collaborative measurement — user opens 3/6/10 tabs by hand, `baseline_capture.sh state <n>` captures meminfo + processes as-is without force-stop

<!-- generated by build_notes.py; do not hand-edit -->
