# D16 — browser restarted between captures, changing the main pid and invalidating one sample

**Section**: D. Measurement and transfer
**Keywords**: browser, captures, changing, churn, consistency, crash, instance, invalidating, logged, main, measurement, meminfo, mid-test, once, process, restarted, retry, sample, seen, settled, stability, states, transfer, transient, truncated, truncation, trusting, verify

## Problem

browser restarted between captures, changing the main pid and invalidating one sample

## Root cause

app instance churn mid-test (no crash logged); transient

## Solution

use only settled states; verify pid + process set consistency before trusting a capture; retry truncated meminfo captures (146-byte truncation seen once)

<!-- generated by build_notes.py; do not hand-edit -->
