# D19 — C1 'verified' was wrong — codec enumeration is not playback

**Section**: D. Measurement and transfer
**Keywords**: advance, beacons, c1, clip, codec, codecs, component_init, confirms, currenttime, decoder, enumeration, fixture, generation, hardwarecodeccapability, honest-benchmark, http, inferred, live, local, logcat, logged, measurement, media, mediacodec, observed, omx.qcom.video.decoder, page, playback, probe, prove, screen, selection, server, session, show, success, surface, three, time-series, transfer, urls, user, verification, verified, video, visible, ways, were, wrong

## Problem

C1 'verified' was wrong — codec enumeration is not playback

## Root cause

r5 probe logged HardwareCodecCapability decoder selection + low CPU, but the probe clip URLs were 404, so no MediaCodec session ever ran; 'verified' was inferred, not observed

## Solution

prove playback three ways: (1) fixture page beacons currentTime to a local HTTP server every 2 s (time-series must advance), (2) logcat must show a live MediaCodec session (OMX.qcom.video.decoder.*, component_init success, surface generation set) in our media pid, (3) user confirms video visible on screen

<!-- generated by build_notes.py; do not hand-edit -->
