# D25 — SurfaceFlinger --latency on ...HomeActivity#0 gave sparse frames (11) and fake jank while the page clearly scrolled

**Section**: D. Measurement and transfer
**Keywords**: active, activity, browser, capture, child, chrome, clearly, composites, content, dumpsys, exists, fake, frames, gave, geckoview, gfxinfo, homeactivity, jank, latency, layer, layers, list, measurement, name, numbers, page, scrolled, smoothness, sparse, surfaceflinger, surfaceview, transfer, trusting, verify, visible

## Problem

SurfaceFlinger --latency on ...HomeActivity#0 gave sparse frames (11) and fake jank while the page clearly scrolled

## Root cause

the #0 layer is the activity/chrome surface; GeckoView web content composites into the child SurfaceView layer, which only exists while the browser is visible

## Solution

capture dumpsys SurfaceFlinger --latency with the SurfaceView layer name from --list (SurfaceView - <pkg>/<activity>#0); verify the surface is active (many frames) before trusting jank numbers; gfxinfo <pkg> = chrome only

<!-- generated by build_notes.py; do not hand-edit -->
