# D27 — C4/C5 smoothness numbers were captured on the wrong layer (activity window, not WebRender content)

**Section**: D. Measurement and transfer
**Keywords**: activity, captured, confirmed, content, dumpsys, frame, gfxinfo, homeactivity, latency, layer, list, main, measurement, numbers, process, real, reports, smoothness, surfaceflinger, surfaceview, thread, timing, transfer, verify, webrender, were, window, wrong

## Problem

C4/C5 smoothness numbers were captured on the wrong layer (activity window, not WebRender content)

## Root cause

gfxinfo reports only the main process (UI thread); the real content layer is a SurfaceView under HomeActivity#0

## Solution

verify via `dumpsys SurfaceFlinger --list` — confirmed `SurfaceView - ...HomeActivity#0`; use SurfaceFlinger latency for content frame timing

<!-- generated by build_notes.py; do not hand-edit -->
