# E13 — 005 v3 failed in CI at values-night/colors.xml:39 while local pristine-file verification passed

**Section**: E. CI quality
**Keywords**: 004-ownership, applies, black, byte-identical, ci, color, colors.xml, combined, edited, failed, fx_mobile_surface, hunk, iceraven, launch, layer, line, local, longer, match, novablack, order, passed, patch-order, patches, pristine, pristine-file, pure, quality, re-verified, references, remaps, repo, rewrites, same, scratch, seamless, splash, target, touches, tree, values-night, verification

## Problem

005 v3 failed in CI at values-night/colors.xml:39 while local pristine-file verification passed

## Root cause

CI applies patches AFTER 004 (seamless launch) and the Iceraven patch layer; 004 already rewrites the splash line (#210340 -> @color/fx_mobile_surface), so 005's combined hunk that edited the same line could not match

## Solution

005 no longer touches the splash line — 004 references fx_mobile_surface, which 005 remaps to novaBlack, so the splash is pure black by reference; re-verified the CI order (pristine -> 004 -> 005 -> 006) in a scratch repo, tree byte-identical to target

<!-- generated by build_notes.py; do not hand-edit -->
