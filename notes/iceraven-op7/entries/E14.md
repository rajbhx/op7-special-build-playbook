# E14 — shellcheck SC2295 on the auto-tag expression broke CI (actionlint)

**Section**: E. CI quality
**Keywords**: actionlint, actionlint+shellcheck, auto-tag, broke, expansion, expression, green, inner, inside, matched, nested, op7_version_name, op7r, parameter-expansion, pattern, quality, quote, quoting, sc2295, shellcheck, workflow

## Problem

shellcheck SC2295 on the auto-tag expression broke CI (actionlint)

## Root cause

nested expansion inside a parameter-expansion pattern: ${OP7_VERSION_NAME%-op7r${REV}} — the inner ${REV} matched as a pattern

## Solution

quote the inner expansion: ${OP7_VERSION_NAME%-op7r"${REV}"}; CI (actionlint+shellcheck) now green

<!-- generated by build_notes.py; do not hand-edit -->
