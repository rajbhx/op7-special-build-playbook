# E15 — local shellcheck/actionlint can't run under this sandbox (shellcheck binary gets killed), so workflow fixes had to round-trip through CI

**Section**: E. CI quality
**Keywords**: actionlint, authoritative, binary, calls, check, ci, container, exec, fast, fixes, gets, killed, local, pre-check, python, quality, rely, round-trip, runs, sandbox, script, shellcheck, step, subprocess, terminates, workflow, yaml.safe_load

## Problem

local shellcheck/actionlint can't run under this sandbox (shellcheck binary gets killed), so workflow fixes had to round-trip through CI

## Root cause

exec sandbox terminates the shellcheck subprocess; actionlint calls it per script

## Solution

rely on the CI actionlint step as the authoritative check (it runs in a container); keep local python yaml.safe_load as a fast pre-check

<!-- generated by build_notes.py; do not hand-edit -->
