# E8 — generated patch 005 lost its `diff --git`/`index` header on first write

**Section**: E. CI quality
**Keywords**: apply, checkout, clean, diff, fresh, generated, generation, git-apply, header, hunk, index, insert, lost, pair, patch, post-004, quality, replacement, sequence, stripped, verify, worktree, write

## Problem

generated patch 005 lost its `diff --git`/`index` header on first write

## Root cause

`sed '1,2d'` stripped the ---/+++ pair before the header replacement could run

## Solution

insert header before first hunk; verify with `git apply --check` on a fresh post-004 checkout, then full 001→005 sequence on a clean worktree

<!-- generated by build_notes.py; do not hand-edit -->
