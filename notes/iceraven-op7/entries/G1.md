# G1 — full journey tables (docs/09, projects/*/README.md) cost agents too many tokens

**Section**: G. Playbook & knowledge
**Keywords**: agents, answer, cost, docs, entries, entry, find, grep-able, index.md, journey, keyword, knowledge, layer, lines, lookup.py, markdown, matching, notes, playbook, problem, projects, ranked, read, readme.md, rendered, search, tables, tokens

## Problem

full journey tables (docs/09, projects/*/README.md) cost agents too many tokens

## Root cause

every entry rendered in one big markdown table; an agent must read all of it to find one answer

## Solution

notes/ layer — INDEX.md keyword->ids map (grep-able, ~150 lines), one entries/<id>.md per problem, lookup.py ranked search; agents read only the matching entry

<!-- generated by build_notes.py; do not hand-edit -->
