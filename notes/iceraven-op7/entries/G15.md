# G15 — connecting a NEW agent/machine to the playbook was a 3-step manual dance (clone, copy skill, write marker) with no single entry point

**Section**: G. Playbook & knowledge
**Keywords**: agent, agents, already-installed, bootstrap, bootstrapped, cleanup, clone, codex, codex_home, command, connecting, copy, dance, deny, entry, existed, existing, exit, flips, idempotent, install, install_skill.sh, installs, keep_clone, knowledge, machine, manual, marker, nothing, one-command, overrides, playbook, playbook_repo, point, refresh, repo, runs, sandboxes, scripts, self-verify, single, skill, skills_dir, source-commit, tolerates, trap, update_skill.sh, updater, writes

## Problem

connecting a NEW agent/machine to the playbook was a 3-step manual dance (clone, copy skill, write marker) with no single entry point

## Root cause

only update_skill.sh existed (for already-installed skills); nothing bootstrapped the first install

## Solution

added scripts/install_skill.sh to the playbook — one command installs the skill from the playbook repo into $CODEX_HOME/skills (or ~/.codex/skills), writes the source-commit marker, then runs the skill's own updater to self-verify; idempotent (existing installs just refresh); KEEP_CLONE/SKILLS_DIR/PLAYBOOK_REPO overrides; cleanup trap tolerates sandboxes that deny rm (never flips exit code)

<!-- generated by build_notes.py; do not hand-edit -->
