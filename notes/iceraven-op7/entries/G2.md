# G2 — keyword auto-extraction produced a noisy index (478 lines of '1.5', '2-core', 'add')

**Section**: G. Playbook & knowledge
**Keywords**: auto-extraction, curated, explicit, index, index.json, kept, keyword, keywords, knowledge, lines, lookup.py, matching, naive, noisy, playbook, prefix-collapse, problem, problem+cause+solution, produced, ranked, search, stopwords, tags, text, tokenization

## Problem

keyword auto-extraction produced a noisy index (478 lines of '1.5', '2-core', 'add')

## Root cause

naive tokenization of problem+cause+solution text

## Solution

curated INDEX from problem text + explicit tags only; full text kept in index.json for ranked lookup.py matching; stopwords + prefix-collapse

<!-- generated by build_notes.py; do not hand-edit -->
