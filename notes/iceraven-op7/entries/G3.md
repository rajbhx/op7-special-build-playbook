# G3 — conversation digests lived only in the build repo, not the playbook

**Section**: G. Playbook & knowledge
**Keywords**: build, conversation, conversations, digests, docs, fetched, fetches, field-notes, generated, knowledge, lists, lived, log.yml, logs, playbook, repo, sessions, sessions.md, skip, slug, summaries, sync, template

## Problem

conversation digests lived only in the build repo, not the playbook

## Root cause

playbook sync fetched only log.yml

## Solution

sync also lists docs/field-notes/sessions and fetches each *.md (skip _template) into _logs/sessions/<slug>/; SESSIONS.md generated with summaries

<!-- generated by build_notes.py; do not hand-edit -->
