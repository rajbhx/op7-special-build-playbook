# G4 — optional tags would be stripped by session_to_notes re-render

**Section**: G. Playbook & knowledge
**Keywords**: canonical, carry, cause, digest, knowledge, optional, parser, playbook, problem, re-render, renderer, session_to_notes, solution, stripped, tags, tooling, wrote, yaml

## Problem

optional tags would be stripped by session_to_notes re-render

## Root cause

canonical renderer wrote only id/problem/cause/solution

## Solution

renderer + digest parser now carry tags: [...] through

<!-- generated by build_notes.py; do not hand-edit -->
