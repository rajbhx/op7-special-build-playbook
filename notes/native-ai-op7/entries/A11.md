# A11 — Redeclaration: ToolResult + 'Cannot find a parameter with this name: id/inputHash'

**Section**: A. CI & build pipeline
**Keywords**: agent, build, cannot, duplicated, find, inputhash, keeps, kotlin, memorymodels.kt, name, naming, parameter, phase, pipeline, record, redeclaration, rename, result, tool, tool-execution, tooloutput, toolresult

## Problem

Redeclaration: ToolResult + 'Cannot find a parameter with this name: id/inputHash'

## Root cause

agent tool result duplicated the Phase 2 DB record name ToolResult (MemoryModels.kt)

## Solution

rename tool-execution result to ToolOutput; the DB record keeps ToolResult

<!-- generated by build_notes.py; do not hand-edit -->
