# A17 — FGS specialUse requires both the permission and the subtype property at targetSdk 34

**Section**: A. CI & build pipeline
**Keywords**: android, build, declare, foreground-service, foreground_service, foreground_service_special_use, foregroundservicetype, introduced, manifest, permission, pipeline, post_notifications, property, property_special_use_fgs_subtype, request, required, requires, runtime, service, specialuse, subtype, targetsdk

## Problem

FGS specialUse requires both the permission and the subtype property at targetSdk 34

## Root cause

Android 14 introduced FOREGROUND_SERVICE_SPECIAL_USE with a required PROPERTY_SPECIAL_USE_FGS_SUBTYPE

## Solution

declare FOREGROUND_SERVICE + FOREGROUND_SERVICE_SPECIAL_USE + POST_NOTIFICATIONS; service foregroundServiceType=specialUse + property; request POST_NOTIFICATIONS at runtime (API 33+)

<!-- generated by build_notes.py; do not hand-edit -->
