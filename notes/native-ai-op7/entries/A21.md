# A21 — Agent unusable: persisted remote provider not re-registered after restart; local model required manual pre-load

**Section**: A. CI & build pipeline
**Keywords**: actionable, agent, auto-load, build, check, compose, context, download, downloader, early-returned, engine-state, generate, gguf, header, in-app, industrial, local, magic, manual, messages, model, models, persisted, picker, pipeline, pre-load, providers, re-registered, real, redesign, registered, remote, required, restart, router, runagent, selection, selector, space, startup, tmp+rename, ui, unloaded, unusable, verify

## Problem

Agent unusable: persisted remote provider not re-registered after restart; local model required manual pre-load

## Root cause

providers registered only on picker selection; runAgent early-returned for unloaded local; messages not actionable

## Solution

re-register persisted remote at startup; auto-load local GGUF on Agent/Generate; add in-app GGUF downloader (space check, magic verify, tmp+rename) + context selector; industrial UI redesign with real engine-state header

<!-- generated by build_notes.py; do not hand-edit -->
