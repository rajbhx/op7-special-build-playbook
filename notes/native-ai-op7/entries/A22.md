# A22 — UI implied free remote models require an API key

**Section**: A. CI & build pipeline
**Keywords**: actionable, actually, amber, anonymous, api-key, authorization, billing, blank, build, clarifies, dialog, free, free-tier, hard-failed, healthy, implied, keys, message, models, omit, per-ip, pipeline, quota, raise, rate-limit, rate-limited, remote, require, shows, source, supported, treats, zen

## Problem

UI implied free remote models require an API key

## Root cause

health() hard-failed on blank key; Zen anonymous billing source is actually supported (per-IP rate-limited)

## Solution

health() treats anonymous as healthy; omit Authorization when key blank; 429 -> actionable message; UI shows amber 'Free · anonymous (rate-limited)' and dialog clarifies keys only raise the quota

<!-- generated by build_notes.py; do not hand-edit -->
