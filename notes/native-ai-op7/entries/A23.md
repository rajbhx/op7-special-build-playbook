# A23 — evaluate LiteRT/LiteRT-LM as a faster local inference backend for OP7

**Section**: A. CI & build pipeline
**Keywords**: acceleration, adr, adr-007, advertises, audited, backend, because, benchmark, build, contended, docs, edge, embedder, evaluate, ex-tflite, export, faster, gate, gguf, google, inference, litert, litert-lm.md, litertlm, llama.cpp, local, lora, memory, needs, on-device, parked, path, phase, pipeline, reference, replacing, revisit, risks, runtime, same-device, source-research, tflite, tool, triggers, upstream, v0.16.0

## Problem

evaluate LiteRT/LiteRT-LM as a faster local inference backend for OP7

## Root cause

LiteRT-LM (Google AI Edge, ex-TFLite) advertises GPU/NPU LLM acceleration, tool use, on-device LoRA; local 1B Q4 is ~1.8 tok/s (contended)

## Solution

audited upstream (v0.16.0); parked as reference only (ADR-007 + docs/source-research/litert-lm.md) because runtime is .tflite/.litertlm with no GGUF path — replacing llama.cpp needs a new export pipeline + second runtime and risks the 1.5 GB gate. Revisit on triggers: memory embedder via TFLite, same-device benchmark win, Phase 9 LoRA

<!-- generated by build_notes.py; do not hand-edit -->
