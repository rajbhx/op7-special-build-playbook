# A29 — JVM tests: ExecutionLayerTestKt > initializationError InvalidTestClassError

**Section**: A. CI & build pipeline
**Keywords**: appended, body, brace, build, class, closing, compile, executionlayertestkt, facade, green, initializationerror, inside, invalidtestclasserror, junit, kotlin, landed, outside, pipeline, pushing, rejects, tail, test, tests, top-level, verify

## Problem

JVM tests: ExecutionLayerTestKt > initializationError InvalidTestClassError

## Root cause

appended JUnit test landed OUTSIDE the class (after the closing brace) -> top-level @Test in the file facade ExecutionLayerTestKt, which JUnit rejects

## Solution

keep every @Test inside the class body; verify with tail -n before pushing (104 tests now green)

<!-- generated by build_notes.py; do not hand-edit -->
