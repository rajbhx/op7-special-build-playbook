# A30 — Termux execution absent at the core execution layer; user wants it core-level, optional Anyclaw/Codex/OpenCode later

**Section**: A. CI & build pipeline
**Keywords**: absent, allow, anyclaw, apps, audit, backend, build, clean-room, codex, com.termux, com.termux.run_command, core, core-level, detection, download, exchange, execution, executionmanager, existed, external, fallback, honest, intent, later, layer, local, localprocessbackend, machine, maxsdk, mediastore, memory, nativeai, one-time, opencode, optional, parked, pipeline, prefs, read_external_storage, ready, reasons, runcommandservice, runid, sdcard, settings, setup, terminalallowlist, terminalenabled, termux, termux-setup-storage, termuxbackend, termuxstatus, tool, tools, user, wants

## Problem

Termux execution absent at the core execution layer; user wants it core-level, optional Anyclaw/Codex/OpenCode later

## Root cause

audit had parked TermuxBackend; only LocalProcessBackend existed

## Solution

TermuxBackend (clean-room): com.termux detection + RunCommandService intent (com.termux.RUN_COMMAND) + file exchange /sdcard/Download/nativeai/<runId>/; TermuxStatus machine with honest reasons; ExecutionManager (Termux READY ? termux : local); Tools settings row; terminalEnabled/terminalAllowlist prefs; READ_EXTERNAL_STORAGE maxSdk 32 + MediaStore fallback for API 33+. One-time user setup: termux-setup-storage + Allow external apps

<!-- generated by build_notes.py; do not hand-edit -->
