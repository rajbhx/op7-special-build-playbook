# A31 — tools invisible/uncontrolled; no inventory contract between ToolRegistry and UI

**Section**: A. CI & build pipeline
**Keywords**: agent, approval, available, badges, build, construction, contract, descriptions, disabled, exposed, inventory, invisible, path, pipeline, privileged, registry, section, settings, shared, shows, terminal, toolbox, tooldescriptor, toolregistry, toolregistry.snapshot, tools, ui, uncontrolled

## Problem

tools invisible/uncontrolled; no inventory contract between ToolRegistry and UI

## Root cause

ToolRegistry exposed only descriptions(); settings had just the terminal row

## Solution

ToolDescriptor + ToolRegistry.snapshot() + shared Toolbox (one construction path for agent and settings); Tools section shows every tool with AVAILABLE/DISABLED/APPROVAL/PRIVILEGED badges

<!-- generated by build_notes.py; do not hand-edit -->
