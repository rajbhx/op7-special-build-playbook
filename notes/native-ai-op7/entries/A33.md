# A33 — no authoritative model status line; quota static until manual health press; no system prompt override; Stats lacked affinity + dynamic budget

**Section**: A. CI & build pipeline
**Keywords**: affinity, affinity_applied, authoritative, auto, available, budget, build, cache, derived, dynamic, hardcoded, health, json, lacked, lines, logged, manual, max-ctx, model, modelstatus.line, native, offline, online, override, persisted, pipeline, press, prompt, quick-send, quota, ready, refresh, result, state, static, stats, status, system, system-prompt, systempromptoverride, thinkingagent

## Problem

no authoritative model status line; quota static until manual health press; no system prompt override; Stats lacked affinity + dynamic budget

## Root cause

no derived status, no auto health refresh, hardcoded system prompt, affinity pin result only logged

## Solution

ModelStatus.line() (READY/AVAILABLE/NO MODEL FILE/ONLINE/OFFLINE) + auto health 60s cache + persisted systemPromptOverride (ThinkingAgent + quick-send) + affinity_applied in native stats JSON + Stats budget/max-ctx lines

<!-- generated by build_notes.py; do not hand-edit -->
