# A34 — schema v3 onUpgrade was a dev reset - every update dropped the whole memory DB (experiences/facts/messages)

**Section**: A. CI & build pipeline
**Keywords**: adds, build, called, dropped, existing, experiences, external, facts, fts5, holds, keeps, memory, messages, migration, migration-retention, oncreate, onupgrade, pipeline, proves, pure, real, reset, rows, schema, schema_version, shared, shortcut, source_chunks_fts, source_collections, source_files, sources, sourceschema, sqlite, sqlite-jdbc, strings, survive, tables, test, triggers, twin, update, whole

## Problem

schema v3 onUpgrade was a dev reset - every update dropped the whole memory DB (experiences/facts/messages)

## Root cause

onUpgrade called DROP TABLE + onCreate as a dev shortcut

## Solution

SCHEMA_VERSION 4 + real migration: v3->v4 keeps all existing tables and only adds source KB tables (source_collections, sources, source_files, source_chunks + external FTS5 source_chunks_fts with triggers); SourceSchema holds pure DDL strings shared with the JVM sqlite-jdbc twin; migration-retention test proves old rows survive

<!-- generated by build_notes.py; do not hand-edit -->
