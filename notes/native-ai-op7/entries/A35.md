# A35 — source registry/updater logic untestable on JVM because MemoryDatabase extends Android SQLiteOpenHelper

**Section**: A. CI & build pipeline
**Keywords**: android, because, bm25, bounds, build, bundles, candidate, changed-blob-only, chunker, conditional, contract, device, eviction, exact, existed, extends, fakesourcestore, fetch, fts5, implementation, implemented, interface, jdbcsourcestore, jvm, logic, memorydatabase, migration, order, org.xerial, pipeline, read-time, registry, retention, running, search, source, sources, sourceschema, sourcestore, sqlite-jdbc, sqliteopenhelper, staleness, storage, test-only, testing, tests, unit, untestable, updater

## Problem

source registry/updater logic untestable on JVM because MemoryDatabase extends Android SQLiteOpenHelper

## Root cause

no storage contract; only the Android implementation existed

## Solution

SourceStore interface implemented by MemoryDatabase (device) and JdbcSourceStore (test-only org.xerial:sqlite-jdbc, bundles FTS5) running the exact SourceSchema DDL; FakeSourceStore for unit tests; tests for migration retention, chunker bounds, candidate staleness order, 304 conditional fetch, changed-blob-only, read-time eviction, BM25 + LIKE search

<!-- generated by build_notes.py; do not hand-edit -->
