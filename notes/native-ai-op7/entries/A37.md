# A37 — chunk overlap disappeared after the word-boundary refactor

**Section**: A. CI & build pipeline
**Keywords**: advanced, asserts, boundary, build, chunker, chunks, consecutive, context, disappeared, guard, loop, losing, overlap, overlapchars, pipeline, progress, refactor, retrieval, share, sourcechunkertest, start, word-boundary, words

## Problem

chunk overlap disappeared after the word-boundary refactor

## Root cause

loop advanced start = end (no overlap), losing boundary context between chunks

## Solution

advance start = end - overlapChars with a progress guard; SourceChunkerTest asserts consecutive chunks share words

<!-- generated by build_notes.py; do not hand-edit -->
