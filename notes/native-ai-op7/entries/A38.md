# A38 — future memory/Source-KB phases lacked a reference for scoped verbatim memory + pluggable retrieval

**Section**: A. CI & build pipeline
**Keywords**: adr-007, backend, benchmark, budget, build, collections, control, docs, drawers, embedder, eviction, fallback, figure, files-chunks, fts5+like, future, give, hierarchy, hybrid, ideas, keyword, lacked, later, longmemeval, maps, matches, memory, memoryprovider, mempalace, mempalace-reference.md, minilm, phases, pipeline, pluggable, pointed, read_time, real, recorded, reference, research, retrieval, rooms, scoped, seam, sizes, source-kb, source-research, sources, sourcestore, user, validates, verbatim, verbatim-growth, wings

## Problem

future memory/Source-KB phases lacked a reference for scoped verbatim memory + pluggable retrieval

## Root cause

user pointed at MemPalace/mempalace (MIT) as a source of ideas for later phases

## Solution

recorded docs/source-research/mempalace-reference.md: scoped hierarchy (wings/rooms/drawers) maps to collections/sources/files-chunks; pluggable backend seam matches SourceStore/MemoryProvider; hybrid keyword fallback validates FTS5+LIKE; embedder sizes (~80 MB minilm) give ADR-007 a real budget figure; LongMemEval R@5 as a future benchmark; keep read_time eviction for verbatim-growth control

<!-- generated by build_notes.py; do not hand-edit -->
