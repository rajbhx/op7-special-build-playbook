# A40 — no observability: AgentEvent lacked timestamps/durations, tool timings and tok/s were untracked, Stats only dumped memoryStats() to the status line

**Section**: A. CI & build pipeline
**Keywords**: acceptance, agentevent, arm64-only, atms, build, cannot, choreographer, contract, data, debugimplementation, device, diagnostics, diagnosticsdialog, diagnosticsprovider, docs, dumped, durationms, durations, emulators, errors, events, first-token, flows, framejankmonitor, jank, lacked, leak, leakcanary, license, lines, load, maestro, memorystats, metrics, observability, per-tool, perf-observability-reference.md, physical, pipeline, plain, prefix, restarts, retries, review, rows, runtimediagnostics, runtimemetrics, source-research, stats, status, strips, timestamped, timestamps, timings, tooling, traceline, untracked, were, x86_64

## Problem

no observability: AgentEvent lacked timestamps/durations, tool timings and tok/s were untracked, Stats only dumped memoryStats() to the status line

## Root cause

no metrics contract; trace events were plain data; no jank/leak tooling

## Solution

RuntimeMetrics (load, first-token, tok/s, per-tool, errors/retries, restarts, jank) + DiagnosticsProvider/RuntimeDiagnostics + DiagnosticsDialog; AgentEvent atMs/durationMs with timestamped trace lines (TraceLine strips the prefix); FrameJankMonitor (Choreographer, no dep); LeakCanary debugImplementation only; .maestro/acceptance flows on the physical device (arm64-only APK cannot run on x86_64 CI emulators); docs/source-research/perf-observability-reference.md + license review rows

<!-- generated by build_notes.py; do not hand-edit -->
