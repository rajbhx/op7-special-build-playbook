# A42 — app SIGSEGV'd (native, fault addr 0x0) in ggml_compute_forward_rope (libggml-cpu-android_armv8.2_2.so) ~2 s after 'agent stopped' while running Qwen2.5-0.5B locally, killing the whole process (engine service runs in the same process)

**Section**: A. CI & build pipeline
**Keywords**: addr, agent, blocking, build, callbackflow, called, calls, cancellation, closes, concurrency, coroutine, corrupting, crash, decode, engine, engine.cancel, fault, finally, fires, flow, generations, ggml_compute_forward_rope, graph, immediately, inside, keeps, killing, libggml-cpu-android_armv8.2_2.so, llama, llama.cpp, llama_context, locally, loop, native, nativecancel, overlapped, paths, pipeline, process, promptly, quick-send, qwen2.5-0.5b, released, reset, rope, runjob, runjob.cancel, running, runs, same, sampled, service, sigsegv, starting, still-decoding, stop, stopped, stoprun, stops, token, true, unwinds, whole, window

## Problem

app SIGSEGV'd (native, fault addr 0x0) in ggml_compute_forward_rope (libggml-cpu-android_armv8.2_2.so) ~2 s after 'agent stopped' while running Qwen2.5-0.5B locally, killing the whole process (engine service runs in the same process)

## Root cause

stopRun() called runJob.cancel() but immediately released running/runJob; local generation is a blocking llama.cpp decode inside callbackFlow, so coroutine cancellation only closes the flow (which fires nativeCancel) and the native loop unwinds on the next sampled token (~0.5-1 s); starting a new run in that window overlapped two generations on one llama_context, corrupting the graph

## Solution

stopRun() now calls engine.cancel() first (stops the native loop promptly) and keeps running=true until the job's finally unwinds, so a new run can never overlap a still-decoding engine; both quick-send and agent paths reset running in finally

<!-- generated by build_notes.py; do not hand-edit -->
