# A43 — startup status line was a stale constant ('Engine library loaded. Put a GGUF in: …') shown even when models/model.gguf existed

**Section**: A. CI & build pipeline
**Keywords**: actual, build, constant, contradiction, download, empty, engine, existed, fixed, gguf, guidance, import, initialized, library, line, loaded, local, local-library, localmodellibrary.scan, model.gguf, models, non-empty, once, pipeline, ready, reflecting, scan, shown, stale, startup, status, string, ux

## Problem

startup status line was a stale constant ('Engine library loaded. Put a GGUF in: …') shown even when models/model.gguf existed

## Root cause

status was initialized once with a fixed string, never reflecting the actual library scan

## Solution

initialize from LocalModelLibrary.scan(): empty -> guidance to Download/import; non-empty -> 'Local library ready: <files>'

<!-- generated by build_notes.py; do not hand-edit -->
