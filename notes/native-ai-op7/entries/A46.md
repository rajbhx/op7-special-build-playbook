# A46 — CI unit-test compile failed: document tests appended outside SourceUpdaterTest class + marker mismatch (doc-1-12 vs 13-byte PDF)

**Section**: A. CI & build pipeline
**Keywords**: appended, back, brace, build, byte, ci, class, closing, compilation, compile, doc-1-12, doc-1-13, document, failed, hand-edited, impossible, inside, kotlin, local, marker, match, mismatch, move, outside, pipeline, placed, real, rely, size, sourceupdatertest, structure, testing, tests, unchanged-doc, unit-test

## Problem

CI unit-test compile failed: document tests appended outside SourceUpdaterTest class + marker mismatch (doc-1-12 vs 13-byte PDF)

## Root cause

hand-edited test file placed new tests after the class closing brace; unchanged-doc marker did not match the real byte size

## Solution

move appended tests back inside the class; use the real marker (doc-1-13); rely on CI test compilation even when a local build is impossible

<!-- generated by build_notes.py; do not hand-edit -->
