# A48 — one CI build per main push made validation serial; every push cancels the previous run

**Section**: A. CI & build pipeline
**Keywords**: branches, build.yml, builds, cancel-in-progress, cancels, ci, concurrency, feature, group, main, merge, on.push, parallel, per-ref, pipeline, previous, push, serial, validated, validation, workflow, workflow-dispatch

## Problem

one CI build per main push made validation serial; every push cancels the previous run

## Root cause

concurrency group build-<ref> with cancel-in-progress; on.push only builds main

## Solution

feature work on ci/* branches + gh workflow run build.yml --ref ci/<branch> = parallel builds (per-ref concurrency); merge the validated branch into main

<!-- generated by build_notes.py; do not hand-edit -->
