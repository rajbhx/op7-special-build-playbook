# A49 — local inference had no runtime abstraction (GGUF/llama-only paths)

**Section**: A. CI & build pipeline
**Keywords**: abstraction, benchmark, build, ci-bundled, dlopen, engine, gated, gguf, inference, inferencebackend, jni, libmnn.so, llama-only, local, mnn, mnnbackend, model, modeldescriptor, on-device, parked, paths, phase-1, pipeline, probe, reference, runtime, runtimekind, seam, single, stayed

## Problem

local inference had no runtime abstraction (GGUF/llama-only paths)

## Root cause

single phase-1 engine; MNN stayed a parked reference

## Solution

RuntimeKind on ModelDescriptor, InferenceBackend seam, MnnBackend dlopen probe over CI-bundled libMNN.so 3.6.1; MNN model inference gated on an on-device benchmark

<!-- generated by build_notes.py; do not hand-edit -->
