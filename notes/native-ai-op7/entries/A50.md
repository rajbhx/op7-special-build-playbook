# A50 — vector index had no owner; embeddings need a validated model

**Section**: A. CI & build pipeline
**Keywords**: benchmark, bm25+hnsw, bounded, build, dormant, embeddingprovider, embeddings, existed, gate, gating, headers, hybrid, index, listed, model, owner, passes, persisted, pipeline, roadmap, runtime, seam, usearch, usearchvectorindex, v2.11.3, validated, vector, vector-lib, vectors, vendored

## Problem

vector index had no owner; embeddings need a validated model

## Root cause

roadmap listed vectors but no runtime or gate existed

## Solution

vendored USearch v2.11.3 headers + vector-lib + USearchVectorIndex (bounded/persisted) + EmbeddingProvider seam; hybrid BM25+HNSW RRF dormant until the benchmark gate passes

<!-- generated by build_notes.py; do not hand-edit -->
