# A57 — 'Agent' button crash: run state lived in the composition; an uncaught exception killed the process

**Section**: A. CI & build pipeline
**Keywords**: agent, authority, build, button, caught, composition, coreerrors, coroutine, crash, data, enginescreen, engineviewmodel, error-log, errors, escaped, exceptions, flow, generate, hook, killed, lived, local-init, one-way, owned, pipeline, process, recorded, single, state, status, surfaced, uncaught, viewmodel

## Problem

'Agent' button crash: run state lived in the composition; an uncaught exception killed the process

## Root cause

run coroutine owned by EngineScreen; exceptions escaped to the process crash hook, not the ERROR surface

## Solution

EngineViewModel = single authority (one-way data flow); agent/generate/local-init exceptions caught, recorded to CoreErrors, surfaced in ERRORS tab + status

<!-- generated by build_notes.py; do not hand-edit -->
