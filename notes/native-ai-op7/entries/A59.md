# A59 — no in-loop tool approval UX; REQUIRES_APPROVAL/PRIVILEGED tools were auto-denied with no consent path

**Section**: A. CI & build pipeline
**Keywords**: allow, alone, always_allow, approval, approvaldecision, auto-denied, build, consent, decided, default, deny, dialog, engineviewmodel, in-loop, injected, onapproval, once, path, pendingapproval, permissions, persists, pipeline, policy-blocked, privileged, request, requires_approval, stays, suspends, toolalwaysallow, toolapprovalrequest, toolexecutor, tools, were

## Problem

no in-loop tool approval UX; REQUIRES_APPROVAL/PRIVILEGED tools were auto-denied with no consent path

## Root cause

ToolExecutor decided alone and the UI never saw the request

## Solution

ToolApprovalRequest/ApprovalDecision + injected onApproval (default DENY); EngineViewModel suspends the run on pendingApproval, persists ALWAYS_ALLOW in toolAlwaysAllow; Allow once / Always / Deny dialog; PRIVILEGED stays policy-blocked

<!-- generated by build_notes.py; do not hand-edit -->
