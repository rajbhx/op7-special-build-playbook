# A60 — no transient feedback: successes/failures only in a scrolling status line; destructive actions had no confirm

**Section**: A. CI & build pipeline
**Keywords**: actions, agent, becomes, build, button, confirm, deletion, destructive, dialogs, failures, feedback, layer, line, loading, model, pillbutton, pipeline, retry, running, scrolling, sharedflow, snackbar, snackbarhost, spinner, status, stop, successes, transient, uievent, undo

## Problem

no transient feedback: successes/failures only in a scrolling status line; destructive actions had no confirm

## Root cause

no snackbar layer, no confirm dialogs

## Solution

UiEvent SharedFlow + SnackbarHost (Retry/Undo); confirm dialog for model deletion; PillButton loading spinner; Agent button becomes Stop while running

<!-- generated by build_notes.py; do not hand-edit -->
