# A66 — run-state races and semantics: double-send could start two runs (flag set inside the coroutine), runAgent had no running guard, stop() double-appended [STOPPED] (stop + the coroutine catch both wrote it), Clear did not reset engine state, primary Agent button was disabled while running so its Stop branch was dead code

**Section**: A. CI & build pipeline
**Keywords**: agent, branch, build, button, buttons, cancels, catch, clear, clearrun, code, coroutine, dead, disabled, double-appended, double-send, elapsed, enabled, engine, enginestate, flags, guards, handled, inside, lastroute, launch, owner, owns, pipeline, places, primary, race, races, resets, return, run-state, runagent, running, running.value, runs, semantics, sendquick, sets, single, start, state, state-machine, status, stays, stop, stopped, synchronously, toggles, wrote

## Problem

run-state races and semantics: double-send could start two runs (flag set inside the coroutine), runAgent had no running guard, stop() double-appended [STOPPED] (stop + the coroutine catch both wrote it), Clear did not reset engine state, primary Agent button was disabled while running so its Stop branch was dead code

## Root cause

flags/status handled in two places (UI + VM) with no single owner

## Solution

sendQuick sets _running synchronously before launch; runAgent guards if (_running.value) return; stop() only cancels (coroutine catch owns [STOPPED]/status); clearRun resets engineState/status/lastRoute/elapsed; primary button toggles Agent<->Stop and stays enabled; Clear disabled while running

<!-- generated by build_notes.py; do not hand-edit -->
