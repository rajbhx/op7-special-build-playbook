# A69 — agent runs were single-shot: prior conversation was stored but never injected as context, and answers never cited which source KB hits were used

**Section**: A. CI & build pipeline
**Keywords**: agent, agentevent.final, agentevent.final.sources, answers, build, carried, citations, cited, context, contextmanager, contextmanager.build, conversation, hits, injected, lines, memory, multi-turn, observations, pipeline, populated, priorconversation, priority, rendered, runs, section, session, single-shot, sources, stored, system, tail, thinkingagent.run, user, were

## Problem

agent runs were single-shot: prior conversation was stored but never injected as context, and answers never cited which source KB hits were used

## Root cause

ContextManager had no conversation section; AgentEvent.Final carried no sources

## Solution

ContextManager.build(conversation=...) with priority system > user > observations > conversation > memory > sources; ThinkingAgent.run(priorConversation=...) fed from the last session's tail; AgentEvent.Final.sources populated from source hits and rendered as SOURCE lines under the answer

<!-- generated by build_notes.py; do not hand-edit -->
