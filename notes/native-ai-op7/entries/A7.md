# A7 — lifecycleScope unresolved in MainActivity (receiver mismatch)

**Section**: A. CI & build pipeline
**Keywords**: activity, android, build, cancel, coroutinescope, dispatchers.main, drop, extension, kotlin, lifecycle, lifecycle-runtime-ktx, lifecycleowner, lifecyclescope, mainactivity, mismatch, ondestroy, pipeline, plain, receiver, supervisorjob, unresolved

## Problem

lifecycleScope unresolved in MainActivity (receiver mismatch)

## Root cause

lifecycleScope is a LifecycleOwner extension; plain Activity is not a LifecycleOwner

## Solution

own CoroutineScope(SupervisorJob() + Dispatchers.Main), cancel in onDestroy, drop lifecycle-runtime-ktx

<!-- generated by build_notes.py; do not hand-edit -->
