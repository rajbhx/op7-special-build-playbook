# A71 — skills were seeded but never wired: DefaultSkills/SkillManager existed with no consumer

**Section**: A. CI & build pipeline
**Keywords**: agent, appends, build, constraints, consumer, created, defaultskills, deterministic, existed, graph, init, model, nowhere, pipeline, prompt, registry, round-trip, runtime, seeded, seeds, skillfor, skillmanager, skills, system, system-prompt, takes, task-matched, tasktype, thinkingagent, toolbox.skillmanager, were, wired, workflow

## Problem

skills were seeded but never wired: DefaultSkills/SkillManager existed with no consumer

## Root cause

skill registry was created nowhere in the runtime graph

## Solution

Toolbox.skillManager (init from app files dir, seeds on first run); ThinkingAgent takes skills and appends the task-matched skill's workflow/constraints to the system prompt (deterministic skillFor(taskType), no model round-trip)

<!-- generated by build_notes.py; do not hand-edit -->
