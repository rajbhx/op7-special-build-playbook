# A73 — benchmark battery existed (ModelBenchmark) but had no UI; approval policy had no per-tool persistence UI

**Section**: A. CI & build pipeline
**Keywords**: approval, battery, benchmark, build, controls, diagnostics, dialog, existed, gains, inventory, measured, model, modelbenchmark, per-tool, persistence, persisting, pipeline, policy, requires_approval, results, selected, toggle, toolalwaysallow, tools, ui, were, wired

## Problem

benchmark battery existed (ModelBenchmark) but had no UI; approval policy had no per-tool persistence UI

## Root cause

benchmark and policy controls were not wired into Diagnostics/TOOL INVENTORY

## Solution

Diagnostics dialog gains Benchmark (per selected model, measured results); REQUIRES_APPROVAL tools get an ALWAYS toggle persisting toolAlwaysAllow

<!-- generated by build_notes.py; do not hand-edit -->
