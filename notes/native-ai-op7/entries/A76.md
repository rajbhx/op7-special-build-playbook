# A76 — the router could substitute a different model than the user's explicit selection without saying why

**Section**: A. CI & build pipeline
**Keywords**: agentevent.routed, build, decisions, different, emits, errors, explicit, fallback, gains, health, line, logs, model, modelrouter.lasterror, pipeline, preferred, providerid, quick-completion, reason, recorded, router, routing, same, saying, selection, shows, silent, status, substitute, surfaced, switching, thinkingagent, trace, transparency, unavailable, user, using, were

## Problem

the router could substitute a different model than the user's explicit selection without saying why

## Root cause

routing decisions were silent; health errors were recorded but never surfaced in trace/status

## Solution

AgentEvent.Routed gains reason; ModelRouter.lastError(providerId); ThinkingAgent emits 'preferred X unavailable (why) — using Y'; trace logs a FALLBACK line and quick-completion status shows the same reason — no silent switching

<!-- generated by build_notes.py; do not hand-edit -->
