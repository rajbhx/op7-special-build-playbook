# A77 — prompt was wiped on process death — only rotation survived

**Section**: A. CI & build pipeline
**Keywords**: booleans, build, clear, covers, death, default, engineviewmodel, golden, keeps, lived, mainactivity, memory, model, persists, pipeline, prefs, process, prompt, purely, remembersaveable, rotation, savedstate, savedstatehandle, selection, state, survival, survived, survives, takes, tests, viewmodel, wiped, working

## Problem

prompt was wiped on process death — only rotation survived

## Root cause

ViewModel state lived purely in memory; only MainActivity used rememberSaveable for two booleans

## Solution

EngineViewModel takes SavedStateHandle (default arg keeps tests working) and persists the prompt key on set/clear; model selection already survives via prefs — golden UX P0 now covers process death

<!-- generated by build_notes.py; do not hand-edit -->
