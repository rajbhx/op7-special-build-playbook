# A79 — skills were wired into the agent but users had no way to manage them (Phase 10 UI missing)

**Section**: A. CI & build pipeline
**Keywords**: agent, badges, build, built-ins, builtin, constraints, create, custom, defaults, delete, dialog, editor, empty, engine, existed, flag, gains, json-persisted, list, manage, missing, panel, phase, phase10, pipeline, protection, purpose, re-seeds, read-only, refuses, seeded, seeds, settings, skillmanager, skillmanager.delete, skills, skillstorage, small, surface, them, tools, ui, users, were, wired, workflow

## Problem

skills were wired into the agent but users had no way to manage them (Phase 10 UI missing)

## Root cause

SkillManager/SkillStorage existed with no create/edit/delete surface and no built-in protection

## Solution

Skill gains a builtin flag (seeds read-only); SkillManager.delete refuses built-ins; ENGINE SETTINGS gains a SKILLS panel with SEEDED/CUSTOM badges, create/edit/delete via a small editor dialog (id/purpose/tools/workflow/constraints), JSON-persisted; empty list re-seeds defaults

<!-- generated by build_notes.py; do not hand-edit -->
