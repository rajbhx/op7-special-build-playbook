# A80 — the verified learning dataset pipeline had no UI and no honest eligibility surfacing

**Section**: A. CI & build pipeline
**Keywords**: build, called, dataset, dedupe, diagnostics, eligibility, existed, export, gains, gate, honest, learning, lora, min-pair, minpairs, nothing, pairs, pipeline, private, public, reasons, report, runs, selflearningpipeline, shows, silently, surfacing, trains, verified

## Problem

the verified learning dataset pipeline had no UI and no honest eligibility surfacing

## Root cause

SelfLearningPipeline existed but nothing called it; minPairs was private

## Solution

Diagnostics gains 'Export dataset': runs export + dedupe + min-pair gate, shows dataset file/pairs/reason and LoRA eligibility reasons (never silently trains); minPairs made public for the report

<!-- generated by build_notes.py; do not hand-edit -->
