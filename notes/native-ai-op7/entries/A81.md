# A81 — Context-bound state (SharedPrefs) had no JVM test coverage

**Section**: A. CI & build pipeline
**Keywords**: androidx.test, build, ci, context-bound, core, coverage, covers, favorites, impact, modelpreferencesrobolectrictest, null, override, persistence, pipeline, prefs, robolectric, selection, semantics, sharedprefs, state, terminal, test, test-only, testimplementation, testing, toggle, toolchain, unittests.includeandroidresources

## Problem

Context-bound state (SharedPrefs) had no JVM test coverage

## Root cause

no Robolectric in the test toolchain

## Solution

testImplementation Robolectric 4.12.2 + androidx.test:core + unitTests.includeAndroidResources; ModelPreferencesRobolectricTest covers selection/override/terminal persistence, null override semantics, favorites toggle — test-only, no APK impact

<!-- generated by build_notes.py; do not hand-edit -->
