# A96 — TOOL INVENTORY was static and disconnected from offline/online + model state

**Section**: A. CI & build pipeline
**Keywords**: build, changes, configured, connectivity, disconnected, inventory, live, model, models-dir, network, networkonline, offline, once, online, open, overlay, pipeline, reasons, recomputed, refreshed, rendered, selection, settings, sheet, snapshot, state, static, surfaced, terminal, termux, toolrows, tools, ui, unavailablereason, web_search

## Problem

TOOL INVENTORY was static and disconnected from offline/online + model state

## Root cause

snapshot() rendered once; no network/termux overlay

## Solution

live toolRows recomputed on network/termux/selection/models-dir changes; networkOnline refreshed every 3s while the settings sheet is open; web_search → NOT CONFIGURED/OFFLINE, terminal reasons surfaced via unavailableReason

<!-- generated by build_notes.py; do not hand-edit -->
