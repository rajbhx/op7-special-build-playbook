# RAIN2 — rainManager ships no native-code entries in badging (lspatch .so live in assets)

**Section**: B. Build, validation & ABI
**Keywords**: abi, arm64-v8a, assets, badging, build, checks, discord, entries, gate, inherited, injected, itself, live, lspatch, manager, native-code, package, patched, patched-output, rainmanager, runtime, ships, testonly, validation

## Problem

rainManager ships no native-code entries in badging (lspatch .so live in assets)

## Root cause

lspatch runtime is injected into the patched Discord APK, not used by the manager itself

## Solution

validation gate for the manager checks package/SDK/testOnly only; patched-output ABI is inherited from Discord (arm64-v8a on OP7)

<!-- generated by build_notes.py; do not hand-edit -->
