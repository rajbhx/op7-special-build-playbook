# DRAFT — generated by project-intake, reviewed + filled for native-ai-op7.
# Schema follows AGENTS.md; every field is present so CI validation passes.
project:
  slug: native-ai-op7              # must match this folder name
  name: "Native AI Engine OP7"     # human-readable name
  description: "Self-learning agentic AI engine (llama.cpp + SQLite FTS5, provider-neutral model routing, OxygenOS UI) for OnePlus 7, arm64-v8a only"
  repo: https://github.com/rajbhx/native-ai-op7   # build repo (public)
  upstream_repo: https://github.com/ggml-org/llama.cpp  # embedded native engine (pinned b10428)
  engine: native                   # detected: C/C++ (CMake) + Android Gradle
  target_device: "OnePlus 7 (GM1901)"   # Snapdragon 855, Adreno 640, 6 GB
  abi: arm64-v8a                   # splits.abi arm64 only (spec §26)
  status: active                    # CI green through provider layer + OxygenOS UI
  maintainer: rajbhx

  # Where this project's field-notes log will live (source for auto-sync).
  field_notes:
    repo: rajbhx/native-ai-op7
    path: docs/field-notes/log.yml

  # Phase status per docs/00-quickstart.md (0=audit ... 10=long-term maintenance)
  phases:
    "0": done            # audit done: llama.cpp API, toolchain, CI baseline, JNI surface
    "1": done            # CI builds stock checkout; APK + badging gate green (A1-A9)
    "2": pending
    "3": done            # GitHub Actions build + badging/ABI/testOnly gates
    "4": pending
    "5": pending
    "6": pending
    "7": pending
    "8": pending
    "9": pending
    "10": pending

  patches_ref: patches/op7/        # thin patches on llama.cpp, never a fork
